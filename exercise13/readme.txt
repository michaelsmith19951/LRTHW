This is the thirteenth exercise that uses the "ARGV" command. "ARGV" is "argument variable", and it holds arguments that the user gives when running the program through the command line. 

Example:

ruby ex13.rb first 2nd 3rd